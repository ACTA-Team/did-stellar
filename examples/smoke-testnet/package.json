{
  "name": "@examples/smoke-testnet",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end smoke test: generate a fresh DID, register it on testnet with your wallet, resolve it back. Exercises the encode/prepare/submit/read paths against the real did-stellar-registry contract.",
  "scripts": {
    "smoke": "tsx src/smoke.ts"
  },
  "dependencies": {
    "@acta-team/did-stellar": "workspace:*",
    "@noble/ed25519": "^2.1.0",
    "@stellar/stellar-sdk": "^15.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
